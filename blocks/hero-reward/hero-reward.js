// eslint-disable-next-line no-unused-vars
export default function decorate(block) {
  // This block uses a CSS background image (SVG) rather than an authored picture element.
  // No DOM restructuring needed — the 3-row structure (heading, description, CTA)
  // is styled directly via CSS.
}
